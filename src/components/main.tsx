const Main: React.FC = ({ children }) => (
  <main className="flex flex-row items-stretch justify-center min-h-screen max-w-7xl mx-auto p-4 md:p-14">
      {children}
  </main>
)

export default Main
