const Main: React.FC = ({ children }) => (
  <main className="flex flex-row items-stretch justify-center min-h-screen max-w-7xl mx-auto px-3 py-14 md:p-14">
      {children}
  </main>
)

export default Main
